URGENT FIX v1.3.5 — 20 photos + clean job queue

Current issues from latest screenshots:

1. Marketplace only uploads 6 photos.
I need it to upload all available photos, up to Facebook's limit of 20.
The vehicle has 20 photos and Facebook allows 20.
Do not limit to 6 anymore.

2. Job Queue is full of old Failed/Cancelled/Auto-expired jobs.
This looks terrible and makes the app feel broken.
The operator should not see a long list of old failed test jobs.
Failed jobs should not block new Publish Now jobs.

====================================================
1. Upload 20 photos by default
====================================================

Change Marketplace photo logic:

- Upload up to 20 photos by default.
- If vehicle has 20 photos, upload 20.
- If vehicle has fewer, upload all available.
- Do not use 6-photo FAST_MODE limit anymore.

Performance rule:
- Download photos in parallel batches of 5.
- Inject all files into Facebook input in one DataTransfer.
- Continue as soon as Facebook accepts the files.
- Do not wait for every thumbnail to fully render before continuing.
- If at least input.files.length > 0 or visible thumbnails appear, photo upload passes.
- Log: "20 photos injected" or "{N} photos injected".

Acceptance:
Facebook shows Photos 20/20 or 20 thumbnails when 20 are available.

====================================================
2. Clean Job Queue UI
====================================================

Normal operators should not see old Failed/Cancelled/Auto-expired jobs by default.

Update Job Queue:

Default filter should show only active/relevant jobs:
- Queued
- Retry
- Assigned
- Claimed
- Publishing
- Opening Facebook
- Downloading Photos
- Uploading Photos
- Waiting For Thumbnails
- Filling Form
- Ready For Review

Hide by default:
- Published
- Cancelled
- Failed older than 10 minutes
- Auto-expired jobs
- Cancelled by operator jobs

Add tabs or filters:
- Active
- Needs Review
- Completed
- Failed History

Default tab: Active.

Acceptance:
The default Job Queue does not show a long wall of red Failed jobs.

====================================================
3. Auto-clean old failed/cancelled jobs
====================================================

Add automatic cleanup:

- Failed jobs older than 10 minutes → move to Failed History / archived
- Cancelled jobs older than 10 minutes → archived
- Auto-expired jobs → archived
- They should not block Publish Now.
- They should not appear in default queue.

Do not delete records from DB.
Just hide/archive them from operator view.

====================================================
4. Publish Now must ignore old failed jobs
====================================================

Before creating a new publish_now job:
- ignore Failed jobs
- ignore Cancelled jobs
- ignore Archived jobs
- ignore old Auto-expired jobs
- only resume same vehicle if there is a fresh active job

Acceptance:
User never needs to click Clear Test Queue for Publish Now to work.

====================================================
5. Remove Retry buttons from old cancelled jobs in default view
====================================================

Retry should only appear for:
- recent failures
- Needs Review jobs
- same vehicle current active job

Old cancelled jobs should not show Retry in the main queue.

====================================================
6. Version/package
====================================================

Package as:
DealerPilot-AI-Extension-v1.3.5.zip

Update:
- manifest version = 1.3.5
- BUILD_LABEL = APP_CONTROLLED_PUBLISHING_1.3.5
- popup shows 1.3.5
- overlay shows 1.3.5

Final acceptance:
- A vehicle with 20 photos uploads 20 photos.
- Job Queue default view is clean.
- Old Failed/Cancelled jobs do not block Publish Now.
- No manual Clear Test Queue required.